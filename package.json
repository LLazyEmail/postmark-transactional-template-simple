{
  "name": "transactional-emails",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone, minimal transactional email template generator. Own setup, no monorepo dependency.",
  "main": "src/index.js",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:real-data": "vitest run tests/integration",
    "lint": "eslint src scripts",
    "lint:fix": "eslint src scripts --fix",
    "format": "prettier --write \"src/**/*.js\" \"scripts/**/*.js\"",
    "format:check": "prettier --check \"src/**/*.js\" \"scripts/**/*.js\"",
    "generate:template": "node scripts/generate-template.js"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^3.0.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=24"
  }
}
