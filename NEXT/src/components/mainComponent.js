/**
 * Stub for the low-level "main" HTML component.
 * Mirrors Work/src/components/ in LLazyEmail/hn_email_template
 * (see mainComponent.unit.test.js in that repo).
 */
function mainComponent() {
  return 'mainComponent';
}

module.exports = { mainComponent };
