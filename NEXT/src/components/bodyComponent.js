/**
 * Stub for the low-level "body" HTML component.
 * Mirrors Work/src/components/ in LLazyEmail/hn_email_template
 * (see body.unit.test.js in that repo).
 */
function bodyComponent() {
  return 'bodyComponent';
}

module.exports = { bodyComponent };
