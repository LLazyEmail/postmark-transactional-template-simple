/**
 * Stub for the low-level "head" HTML component.
 * Mirrors Work/src/components/ in LLazyEmail/hn_email_template
 * (see headComponent.unit.test.js in that repo).
 *
 * Returns just the component's own name instead of real HTML markup —
 * useful as a structural placeholder while the real implementation
 * is still being ported over.
 */
function headComponent() {
  return 'headComponent';
}

module.exports = { headComponent };
