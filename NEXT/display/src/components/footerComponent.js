/**
 * Stub for the low-level "footer" HTML component.
 * Mirrors Work/src/components/ in LLazyEmail/hn_email_template
 * (see footer.unit.test.js in that repo).
 */
function footerComponent() {
  return 'footerComponent';
}

module.exports = { footerComponent };
