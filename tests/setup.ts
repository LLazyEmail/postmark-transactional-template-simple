// tests/setup.ts
// Add Vitest config for this file in vitest.config.ts (setupFiles: ['./tests/setup.ts']).
// Currently just a placeholder for global matchers / env guards.

import { afterEach } from 'vitest';

afterEach(() => {
  // Reserved for global cleanup (e.g., vi.restoreAllMocks()).
});